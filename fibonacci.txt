#include <stdio.h>
int main(){
int i,n;
int a=0,b=1, next;
printf("enter a fibonacci numbers:");
scanf("%d",&n);
printf("fibonacci sequence:");
    for(i = 0; i < n; i++) {
        printf("%d ", a);
        next = a + b;  
        a = b;         
        b = next;
    }
return 0;
}
